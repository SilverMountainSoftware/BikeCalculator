{
  "name": "bike-calculator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json && shx mkdir -p dist && shx cp app.js index.html styles.css dist/",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "shx": "^0.4.0",
    "typescript": "^5.0.0"
  }
}
