# Lessons

- When the user explicitly switches from planning to implementation, stop treating plan-mode constraints as active and proceed with execution.
- When a feature asks for the maximum valid numeric value, search the full allowed numeric range unless the user explicitly limits the search space.
- When drivetrain lists already define the available gears, do not keep duplicate single-gear inputs that can drift out of sync with those lists.